{
  "_comment": "Pass with: mcphost --config examples/clients/mcphost.json --model ollama:granite4.1:3b -p 'your prompt'. mcphost also reads ~/.mcphost.yml by default.",
  "mcpServers": {
    "rocannon": {
      "command": "uv",
      "args": [
        "run",
        "--directory", "/path/to/rocannon",
        "rocannon", "mcp", "serve",
        "--profile", "/path/to/rocannon/examples/quickstart/profile.yml"
      ]
    }
  }
}
